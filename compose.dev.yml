services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: run
      POSTGRES_PASSWORD: runpass
      POSTGRES_DB: run_dashboard
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - db_dev:/var/lib/postgresql/data
volumes:
  db_dev:
